// module.exports = {
//     //让编译css时进行输出
//     plugins: [require('@postcss-plugins/console')],
// };

//上面这样配置的话，开启报错，还不具体了解什么原因，所以注释掉了

// module.exports = {
//     plugins: [require('postcss-preset-env')],
// };

//只需要在vite.config.js文件中配置就可以了，不用在这里配置，因为我实践过生效
